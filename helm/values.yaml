replicaCount: 1

image:
  repository: your-registry/prometheus-mcp-server
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

env:
  PROMETHEUS_BASE_URL: "http://prometheus:9090"

resources: {}

